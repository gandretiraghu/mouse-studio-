// MouseStudioGUI views (Phase 5+), see TDD §4 and §19:
//   App/          — MouseStudioApp entry, AppState, IPCClient
//   Rules/        — RulesView (virtualized, 1000+), RuleEditorView, RuleCreationWizard,
//                   TriggerPicker, ActionBrowser (categories + search), ConflictBadge
//   Tester/       — MouseTesterView (Detect Mouse + live highlight)
//   Profiles/     — ProfilesView
//   ImportExport/ — ImportExportView, Backup/Restore UI
//   Logs/         — LogsView
// Placeholder to document intended file layout.
