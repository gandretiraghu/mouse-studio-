// MouseStudioService components (Phase 4):
//   EngineHost.swift       — owns Core engine, lifecycle, learning mode
//   IPCServer.swift        — XPC server; validates client code signature (TDD §13)
//   MenuBarController.swift — status item, profile quick-switch, pause/resume
// Placeholder to document intended file layout.
